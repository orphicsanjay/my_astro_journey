import 'package:flutter/material.dart';

class ProfileFormSpacer extends StatelessWidget {
  const ProfileFormSpacer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 16);
  }
}
